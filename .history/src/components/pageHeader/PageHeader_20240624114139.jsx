import React, { memo } from 'react'

import HeaderWrapper from './style'

const PageHeader = memo(() => {
  return <HeaderWrapper className="content_mid"> Booksure</HeaderWrapper>
})

export default PageHeader
