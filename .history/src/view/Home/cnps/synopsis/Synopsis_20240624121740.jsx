import React, { memo } from 'react'

import SynopsisWrapper from './style'

const Synopsis = memo(() => {
  return <SynopsisWrapper className="content_mid">Synopsis</SynopsisWrapper>
})

export default Synopsis
