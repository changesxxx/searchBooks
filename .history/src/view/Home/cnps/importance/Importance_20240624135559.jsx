import React, { memo } from 'react'

import ImportanceWrapper from './style'

const Importance = memo(() => {
  return (
    <ImportanceWrapper className="content_mid">Importance</ImportanceWrapper>
  )
})

export default Importance
